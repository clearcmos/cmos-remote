"""Test package for the Desk Remote server.

Present so the test modules can share captured command output through a normal
import (`from tests.captured_output import ...`) instead of relying on
implicit namespace-package resolution.
"""
